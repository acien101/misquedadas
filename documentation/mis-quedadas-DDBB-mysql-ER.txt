Hola:

Supongo que esta es la versin previa a la reunin de hoy. Segn lo que hablamos, creo que 

CREATE TABLE peña (
        nombre varchar(30) DEFAULT NULL,
        id_gente int(4) DEFAULT NULL
);

se desdobla en

CREATE TABLE peña (
  nombre varchar(30) NOT NULL,
  fecha_creacion date DEFAULT NULL, -- ejemplo de atributo
  -- otros atributos que queramos poner
  PRIMARY KEY (nombre)
);

CREATE TABLE pertenece -- u otro nombre
(
  nombre varchar(30) NOT NULL, 
  id_gente int(4) NOT NULL 
);
para cuadrar con lo que se cuenta en teora del tema 5 y partir de un diagrama ER como el de la foto que mand antes.
Adems, en 
CREATE TABLE asistencia (
  id_quedada int(4) DEFAULT NULL,     
  id_gente int(4) DEFAULT NULL       
); 
creo que los dos campos deberan ser NOT NULL para que las entradas de la tabla tengan sentido ¿no?


